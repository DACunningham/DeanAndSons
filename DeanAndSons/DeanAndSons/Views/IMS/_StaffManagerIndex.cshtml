@model DeanAndSons.Models.IMS.ViewModels.StaffManagerIndexIMSViewModel

@* ********** Director's ********** *@

<table class="table">
    <tr>

        <th>
            UserName
        </th>
        <th>
            Forename
        </th>
        <th>
            Surname
        </th>
        <th>
            Superior
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model.Directors)
    {
        <tr>
            <td class="col-md-3">
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td class="col-md-2">
                @Html.DisplayFor(modelItem => item.Forename)
            </td>
            <td class="col-md-2">
                @Html.DisplayFor(modelItem => item.Surname)
            </td>
            <td class="col-md-3">
                @Html.DisplayFor(modelItem => item.Superior.Forename)
                @Html.DisplayFor(modelItem => item.Superior.Surname)
            </td>
            <td class="col-md-1">
                @Html.ActionLink("Edit", "StaffManagerEdit", new { id = item.Id }) |
                <a id=@item.Id class="deleteStaff" href="#deleteModal" data-toggle="modal" data-target="#deleteModal">Delete</a>
            </td>
        </tr>
    }

</table>

@* ********** Manager's ********** *@

<table class="table">
    <tr>

        <th>
            UserName
        </th>
        <th>
            Forename
        </th>
        <th>
            Surname
        </th>
        <th>
            Superior
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model.Managers)
    {
        <tr>
            <td class="col-md-3">
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td class="col-md-2">
                @Html.DisplayFor(modelItem => item.Forename)
            </td>
            <td class="col-md-2">
                @Html.DisplayFor(modelItem => item.Surname)
            </td>
            <td class="col-md-3">
                @Html.DisplayFor(modelItem => item.Superior.Forename)
                @Html.DisplayFor(modelItem => item.Superior.Surname)
            </td>
            <td class="col-md-1">
                @Html.ActionLink("Edit", "StaffManagerEdit", new { id = item.Id }) |
                <a id=@item.Id class="deleteStaff" href="#deleteModal" data-toggle="modal" data-target="#deleteModal">Delete</a>
            </td>
        </tr>
    }

</table>

@* ********** Agent's ********** *@

<table class="table">
    <tr>

        <th>
            UserName
        </th>
        <th>
            Forename
        </th>
        <th>
            Surname
        </th>
        <th>
            Superior
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model.Agents)
    {
        <tr>
            <td class="col-md-3">
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td class="col-md-2">
                @Html.DisplayFor(modelItem => item.Forename)
            </td>
            <td class="col-md-2">
                @Html.DisplayFor(modelItem => item.Surname)
            </td>
            <td class="col-md-3">
                @Html.DisplayFor(modelItem => item.Superior.Forename)
                @Html.DisplayFor(modelItem => item.Superior.Surname)
            </td>
            <td class="col-md-1">
                @Html.ActionLink("Edit", "StaffManagerEdit", new { id = item.Id }) |
                <a id=@item.Id class="deleteStaff" href="#deleteModal" data-toggle="modal" data-target="#deleteModal">Delete</a>
            </td>
        </tr>
    }

</table>